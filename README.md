Download the C++ file. Open the folder containing the file on terminal and run the following command:

g++ TicTacToe.cpp -o TTT
./TTT

# Or, after creating the TTT exectuable file, double click on its icon to run it
Enojoy the game
